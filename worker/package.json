{
  "name": "yonmoque-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "npm --prefix ../client ci && npm --prefix ../client run lint && npm --prefix ../client run build",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute yonmoque --local --file schema.sql",
    "db:remote": "wrangler d1 execute yonmoque --remote --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
